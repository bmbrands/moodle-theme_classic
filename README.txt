This is a child theme of the Boost theme that replaces the clean theme.

It supports:

Bootstrap 4 Sass and JavaScript
The 1, 2 and 3 column grid
The settings and navigation blocks
Allow the use of "classic" presets

It does not support:

docking
css written in less
